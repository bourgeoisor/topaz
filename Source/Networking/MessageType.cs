namespace Topaz.Networking
{
    public enum MessageType {
        ConnectionInfo,
        PlayerConnected,
        PlayerDisconnected,
        PlayerMoved,
        MapInfo,
        MapChanged
    }
}
